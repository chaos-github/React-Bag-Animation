# 开发环境设置指南

## 🚀 快速开始

### 1. 安装依赖

在项目根目录运行：

```bash
npm install
```

如果遇到权限问题，可以尝试：

```bash
npm install --legacy-peer-deps
```

### 2. 启动开发服务器

```bash
npm run demo
```

这会：
- 启动 Vite 开发服务器
- 自动打开浏览器到 `http://localhost:3000`
- 启用热重载（修改代码自动刷新）

### 3. 开始开发

现在你可以：
- ✅ 实时预览组件效果
- ✅ 修改 `src/BagAnimation.tsx` 查看变化
- ✅ 测试遮罩层、动画等所有功能
- ✅ 在 `demo/App.tsx` 中修改示例

## 📁 文件结构

```
bag-Animation-ui/
├── demo/                    # 开发演示
│   ├── index.html          # HTML 入口
│   ├── main.tsx            # React 入口
│   ├── App.tsx             # 示例应用
│   ├── tailwind.config.js  # Tailwind 配置
│   └── postcss.config.js   # PostCSS 配置
├── src/                     # 组件源码
│   ├── BagAnimation.tsx    # 主组件
│   ├── index.ts            # 导出文件
│   ├── assets.ts           # 资源路径
│   └── index.css           # 样式文件
├── public/                  # 静态资源
│   └── assets/             # 图片资源
└── vite.config.demo.ts     # Vite 配置
```

## 🎯 使用说明

### 开发组件

1. 修改 `src/BagAnimation.tsx`
2. 保存文件
3. 浏览器自动刷新，查看效果

### 测试不同场景

在 `demo/App.tsx` 中修改 props：

```tsx
<BagAnimation
  isAnimating={isAnimating}
  setIsAnimating={setIsAnimating}
  doneFunction={handleDone}
  showMask={true}        // 测试遮罩
  maskOpacity={0.8}     // 测试透明度
  maskBlur={12}          // 测试模糊度
/>
```

## ⚠️ 注意事项

1. **确保资源存在**：`public/assets` 目录中需要有图片文件
2. **Tailwind CSS**：组件使用了 Tailwind 类名，demo 已配置 Tailwind
3. **热重载**：修改源码后会自动重新编译和刷新

## 🛠️ 其他命令

```bash
# 构建组件（用于发布）
npm run build

# 构建演示页面
npm run demo:build

# 监听组件构建（用于开发）
npm run dev
```

## 🐛 问题排查

### 端口被占用

如果 3000 端口被占用，修改 `vite.config.demo.ts` 中的端口号。

### 样式不生效

确保 Tailwind CSS 已正确安装和配置。

### 图片不显示

检查 `public/assets` 目录中是否有对应的图片文件。

